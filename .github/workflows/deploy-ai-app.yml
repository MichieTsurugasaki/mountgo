name: Deploy AI Pages Functions (proxy)

on:
  push:
    branches: [ main ]
    paths:
      - 'pages_functions/**'
      - '.github/workflows/deploy-ai-app.yml'
  workflow_dispatch:
    inputs:
      project_name:
        description: "Cloudflare Pages project name (default: ai-app)"
        required: false
        default: "ai-app"

jobs:
  deploy:
    runs-on: ubuntu-latest
    permissions:
      contents: read
      deployments: write
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Ensure Pages project exists (ai-app)
        uses: cloudflare/wrangler-action@v3
        with:
          apiToken: ${{ secrets.CF_API_TOKEN }}
          accountId: ${{ secrets.CF_ACCOUNT_ID }}
          workingDirectory: pages_functions
          command: >-
            pages project create ${{ inputs.project_name || 'ai-app' }}
            --production-branch main
            --skip-guided-setup
        continue-on-error: true

      - name: List existing Pages projects (debug)
        uses: cloudflare/wrangler-action@v3
        with:
          apiToken: ${{ secrets.CF_API_TOKEN }}
          accountId: ${{ secrets.CF_ACCOUNT_ID }}
          workingDirectory: pages_functions
          command: pages project list

      - name: Deploy Pages Functions
        id: deploy
        uses: cloudflare/wrangler-action@v3
        with:
          apiToken: ${{ secrets.CF_API_TOKEN }}
          accountId: ${{ secrets.CF_ACCOUNT_ID }}
          workingDirectory: pages_functions
          command: >-
            pages deploy public
            --project-name ${{ inputs.project_name || 'ai-app' }}
            --branch main
            --commit-dirty=true

      - name: Output deployment URL
        if: always()
        run: |
          echo "Deployment finished."
          echo "Note: Configure GEMINI_API_KEY in Cloudflare Pages (Project: ai-app) for the API routes to work."
