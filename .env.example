# === Gemini 接続設定（開発用）===
# 推奨: WebではAPIキーをクライアントに置かず、Cloudflare Workers などのプロキシを使います。
# 下記 URL はあなたの Workers/サーバのエンドポイントに差し替えてください。
# 例）https://your-worker.yourdomain.workers.dev/gemini
# 例）https://your-worker.yourdomain.workers.dev/gemini/stream

# 非ストリーミング（POST JSON: { system, user, context } -> { text })
GEMINI_API_URL=
# ストリーミング（SSE: text/event-stream）
GEMINI_STREAM_URL=

# 直接 Gemini API を使う場合のみ設定（開発用途）。
# Web クライアントにAPIキーを含めるのは非推奨です。必ず自己責任で、公開ビルドでは使わないでください。
GEMINI_API_KEY=
